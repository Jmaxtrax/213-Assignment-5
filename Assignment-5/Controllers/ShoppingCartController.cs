using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;

namespace Assignment_5.Controllers
{
    public class ShoppingCartController : Controller
    {
        //[HttpPost]
        //public IActionResult AddToCart([FromBody] List<int> songIds)
        //{
            // Implement logic to add selected songs to the shopping cart
            // You may use session, cookies, or a database to store cart information
            // Return a response, such as the updated cart items
            //var cartItems = HttpContext.Session.Get<List<CartItem>>("ShoppingCart");// Logic to get or update the shopping cart items
            //return Json(cartItems);
        //}
            //public IActionResult ViewCart()
          //  {
                //var cart = HttpContext.Session.Get<List<CartItem>>("ShoppingCart") ?? new List<CartItem>();
                //return View(cart);
         //   }

        //public IActionResult Checkout()
        //{
            // Implement logic for the checkout page
       //     return View();
       // }
    }
}
